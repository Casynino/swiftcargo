/**
 * The share card for /track/<reference> — the link that actually gets pasted
 * into WhatsApp. The same drawing as /track, and deliberately the same for
 * every reference: see lib/track-share-card.tsx for why nothing about the
 * consignment appears on it.
 */
export { alt, size, contentType, default } from "@/lib/track-share-card";

export const runtime = "nodejs";
